# Position Watch — locked dashboard

This repository only hosts a passcode-locked page on GitHub Pages. The page is
encrypted (AES-256-GCM, key derived from a passcode) before it is pushed, so
nothing here is readable without the passcode. It is rebuilt every morning by
a private project.
